# US Kaggle Secondary Benchmark Data

`raw/Insurance_Dataset_US_Medical_Charges_1338.csv` is the unchanged input for the secondary US medical-charge benchmark.

It is not used to train the NSS primary model and is not merged with LIAM or Malaysian hospital-pricing data. Runtime artifacts are stored under `Backend/models/us_kaggle/` and return USD source-dataset context only.
